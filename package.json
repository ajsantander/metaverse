{
  "name": "metaverse",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run watch & npm run serve",
    "serve": "parcel serve src/index.html --port 3000 --out-dir build",
    "build": "parcel build src/index.html --out-dir build",
    "watch": "parcel watch src/index.html --out-dir build --no-cache --no-hmr"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/polyfill": "^7.8.7",
    "axios": "^0.19.2",
    "parcel-bundler": "1.9.7",
    "three": "^0.114.0"
  }
}
